import { Open_Sans, Lalezar} from 'next/font/google'
 
export const open_sans = Open_Sans({
  subsets: ['latin'],
  display: 'swap',
})
 
export const lalezar = Lalezar({
  subsets: ['latin'],
  display: 'swap',
  weight: "400"
})